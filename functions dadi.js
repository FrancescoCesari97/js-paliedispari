//- usando una funzione generiamo un numero per il computer da 1 a 5

function computerNumber() {
  let numberComputer = Math.floor(Math.random() * (5 - 1 + 1)) + 1;
  console.log(numberComputer);
}

function sumNumber() {
  let somma = numero + computer;
}
